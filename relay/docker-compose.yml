services:
  relay:
    build: .
    ports:
      - "4000:4000"
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - relay-data:/app/data
    environment:
      - RELAY_PORT=4000
      - CORS_ORIGIN=*

volumes:
  relay-data:
